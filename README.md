# 🎬 Video Tool PRO

เครื่องมือดาวน์โหลดวิดีโอและรวมไฟล์ (M3U8/MP4) ที่ใช้งานง่าย พร้อมหน้าจอ GUI สวยงาม
(A simple and powerful Video Downloader & Merger with a modern ImGui interface)

## ✨ ฟีเจอร์หลัก (Features)
- 📥 **Video Downloader**: รองรับการโหลดวิดีโอผ่าน URL และไฟล์ M3U8
- 🔗 **Video Merger**: รวมไฟล์วิดีโอหลายๆ ไฟล์เข้าด้วยกันอย่างรวดเร็ว
- 🖼️ **Modern GUI**: หน้าจอใช้งานง่าย พัฒนาด้วย Dear ImGui (imgui_bundle)
- ⚡ **Portable**: รองรับการ Build เป็นไฟล์เดียว (.exe) ไม่ต้องติดตั้ง Python ในเครื่องปลายทาง

## 🛠️ ความต้องการของระบบ (Prerequisites)
ก่อนเริ่มต้นใช้งาน คุณจำเป็นต้องมี:
- **Python 3.10 ขึ้นไป**
- **FFmpeg**: จำเป็นสำหรับการประมวลผลวิดีโอ 
  - [ดาวน์โหลด FFmpeg ได้ที่นี่](https://github.com/BtbN/FFmpeg-Builds/releases) (ดาวน์โหลดแบบ essentials-build หรือ full-build แล้วนำ `ffmpeg.exe` มาวางในโฟลเดอร์โปรเจกต์)
 
## 🛠️ การติดตั้งสำหรับนักพัฒนา (Installation)
1. คลอนโปรเจกต์ (Clone the repository):
   ```bash
   git clone [https://github.com/m4rtkl2000-art/VideoToolPro.git](https://github.com/m4rtkl2000-art/VideoToolPro.git)
2. ติดตั้ง Library ที่จำเป็น:
   ```bash
   pip install imgui-bundle requests tkinter
3. วางไฟล์ ffmpeg.exe ไว้ในโฟลเดอร์หลัก
   ```bash
   [https://github.com/BtbN/FFmpeg-Builds/releases](https://www.ffmpeg.org/download.html)

## 📦 การ Build เป็นไฟล์ .exe
1. ให้ build โดยใช้คำสั่ง
   ```bash
   pyinstaller main.spec
2. จากนั้นจะได้ไฟล์ .exe อยู่ตำแหน่ง ..\dist\VideoToolPro.exe
3. สามารถใช้งานได้เลย

## 🚀 วิธีการใช้งานผ่าน Python (Usage for Python Users)
หากคุณไม่ต้องการใช้ไฟล์ .exe และต้องการรันโปรแกรมผ่าน Source Code โดยตรง ให้ทำตามขั้นตอนดังนี้:
1. เตรียมความพร้อม (Prerequisites)
   - ติดตั้ง Python 3.10 หรือเวอร์ชันที่ใหม่กว่า
   - ดาวน์โหลด FFmpeg (สำคัญมาก)
     -   ดาวน์โหลดไฟล์ที่เป็น win64-gpl.zip หรือ essentials
     -   แตกไฟล์แล้วก๊อปปี้ไฟล์ ffmpeg.exe มาวางไว้ในโฟลเดอร์เดียวกับไฟล์ main.py
2. ตั้งค่าสภาพแวดล้อม (Environment Setup)
   ```bash
   # สร้าง venv
   python -m venv .venv

   # เปิดการใช้งาน venv (Windows)
   .venv\Scripts\activate
3. ติดตั้ง Library ที่จำเป็น
   ```bash
   pip install imgui-bundle requests tkinter
4. เริ่มใช้งานโปรแกรม
   เมื่อเตรียมไฟล์และ Library ครบแล้ว ให้สั่งรันโปรแกรมด้วยคำสั่ง:
   ```bash
   python main.py

## 📂 โครงสร้างโปรเจกต์ (Project Structure)
   โครงสร้าง
   ```bash
   VideoToolPro/
   ├── main.py              # ไฟล์หลักสำหรับรันโปรแกรม
   ├── ffmpeg.exe           # (ต้องนำมาวางเอง) สำหรับประมวลผลวิดีโอ
   ├── version_info.txt     # ข้อมูลเวอร์ชันสำหรับ Build .exe
   ├── main.spec            # ไฟล์ตั้งค่าสำหรับ PyInstaller
   ├── fonts/               # เก็บไฟล์ฟอนต์ (.ttf)
   │   └── ...
   ├── function/            # โฟลเดอร์เก็บโมดูลฟังก์ชันหลัก
   │   ├── downloader.py
   │   ├── merger.py
   │   └── converter.py
   └── assets/              # เก็บไอคอนโปรแกรม (icon.png)
